#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);
    
    while (t--) {
        int x,y,z;
        scanf("%d", &x);
       y=(2*x);
       z=(5*y);
       printf("%d\n",z);
    }
}
